package src.Fundamentals;

public class Comparison {
    public static void main(StringManipulation[] args) {
        
        int x = 6;
        int y = 5;

        boolean result = x > y;
        // boolean result = x >= y;
        // boolean result = x <= y;
        // boolean result = x == y;
        // boolean result = x != y;
        // logical operators
        // and, or and not 
        // and = &, | = or and ! = not
        // other one is &&, || and ! is called shortcircuit

        System.out.println(result);
    }
}
